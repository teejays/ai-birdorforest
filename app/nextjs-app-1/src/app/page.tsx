import { FileUploadForm } from "@/components/form/file-upload-form";

const Home = (props: {}) => {
  return <Form />;
}

export default Home;

const Form = (props: {}) => {
    return <FileUploadForm />;
}